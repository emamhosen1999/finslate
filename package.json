{
  "name": "finslate",
  "version": "1.0.0",
  "private": true,
  "description": "FinSlate – Personal Finance PWA (BDT, Modern Slate). Mobile-first React + Vite frontend with Express + MySQL backend.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "nodemon server/index.js",
    "dev:client": "cd client && vite",
    "build": "cd client && vite build",
    "build:prod": "cd client && npm install --include=dev && npm run build",
    "start": "node server/index.js",
    "migrate": "node server/db/migrate.js",
    "install:all": "npm install && cd client && npm install",
    "lint:server": "echo 'no server lint configured'",
    "lint:client": "cd client && vite build --mode production --base=/ >/dev/null && echo client builds"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.0.1",
    "express-mysql-session": "^3.0.3",
    "express-session": "^1.18.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.3",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
